## 执行准则

- 用中文进行对话交流，用英文输出文档内容。
- 如果有不确定的，向我提问，直到百分之百确定我的意思和要做的是什么。
- 不要做要求之外的任何事情，如果认为必要，直接提出询问。
- 查看代码时先查看 doc/architecture.md 文件对整个项目的架构进行了解，然后阅读所有必要的文件，确保你知道足够的上下文信息再进行代码改动。
- 如果有改动代码结构，必须同步更新 doc/architecture.md 文件。
- 对于 TypeScript 的注释要求：输出代码时务必写**恰到好处**的注释——文件顶部给出简短模块注释（用途/依赖/关键不变式）；为所有 **export** 的函数/类/类型写 TSDoc（1–2 句摘要，必要的 `@param/@returns/@throws`，示例仅在不直观时）；行内注释只解释**难点与原因**（算法取舍、边界/不变式、性能/安全/兼容性），不要复述代码本身；注释精炼、及时更新，能用清晰命名就别写废话；使用 `TODO(owner, YYYY-MM-DD)` / `FIXME(...)` 规范；提交前自检：有文件头、所有导出有 TSDoc、复杂片段有一句解释、无陈旧或噪音注释。